
**what is .gitkeep used for**

```bash
some_folder
.gitkeep
```

Placeholder file to make git track an otherwise-empty directory — git doesn't store empty folders, so .gitkeep (any filename works; it's just a convention, not a git feature) forces the directory to exist in the repo.